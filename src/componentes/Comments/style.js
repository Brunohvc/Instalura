import { StyleSheet } from 'react-native'

const style = StyleSheet.create({
    inline: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    imgSend: {
        height: 30,
        width: 30,
        marginRight: 5
    }
});

export default style;